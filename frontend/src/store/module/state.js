export default function () {
  const textMode = false
  const categoryMode = false

  // SignUp
  let user_info = {}
  const hash_tags = []
  return {
    textMode,
    categoryMode,
    user_info,
    hash_tags
  }
}